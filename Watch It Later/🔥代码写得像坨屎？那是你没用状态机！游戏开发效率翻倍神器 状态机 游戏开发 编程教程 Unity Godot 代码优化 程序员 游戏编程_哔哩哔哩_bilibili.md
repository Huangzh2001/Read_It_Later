---
url: https://www.bilibili.com/video/BV1KkikBHE2S/?spm_id_from=333.337.search-card.all.click&vd_source=06168f390bae49c4867767c52a20e87c
tags:
  - video
status: readed
date: 2026-04-07T10:49:57+08:00
---
![🔥代码写得像坨屎？那是你没用状态机！游戏开发效率翻倍神器 #状态机 #游戏开发 #编程教程 #Unity #Godot #代码优化 #程序员 #游戏编程_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1KkikBHE2S/?spm_id_from=333.337.search-card.all.click&vd_source=06168f390bae49c4867767c52a20e87c)
🔥代码写得像坨屎？那是你没用状态机！游戏开发效率翻倍神器 #状态机 #游戏开发 #编程教程 #Unity #Godot #代码优化 #程序员 #游戏编程
https://www.bilibili.com/video/BV1KkikBHE2S/?spm_id_from=333.337.search-card.all.click&vd_source=06168f390bae49c4867767c52a20e87c
xcount 2026-01-04 10:15:21

当我创建我的第一个状态机时，仿佛整个新世界向我敞开，我从未想过如此简单的结构竟能帮助我写出极其整洁的代码，这也非常容易扩展，在我学习状态机之前，我的角色控制器代码看起来像这样，我的玩家可以静止、左移或右移，跳跃并落回地面，只是一个简单的小脚本对吧，但当我想要添加新机制时，比如下蹲，我不能仅检查下蹲输入并播放下蹲动画，因为脚本中更早的动画调用会导致动画重置到第一帧，所以像所有新手开发者一样，我添加了一个新变量来跟踪，玩家是否正在下蹲并在，if语句中阻止某些代码执行，这段代码仍然相对容易理解，但当我添加更多机制如俯冲，二段跳或墙壁跳跃时，每个机制都带来了新变量，我的脚本最终充斥着大量嵌套，if语句或随着时间推移排列的条件，变量和条件的累积让代码变得混乱且难以调试，但这就是状态机的作用，它的应用不仅限于游戏或角色控制器，在各种系统中广泛应用，无论你使用Unity，好的，不过，还是Unreal，我是说，甚至在Scratch里做过一个，状态机会帮助我们写出整洁易懂的代码，既然你已了解为何需要状态机，下一个问题是，状态机如何工作，以及如何构建它，状态机是一个描述系统行为的机制，在这里我的情况是玩家，但对你可能是简单敌方AI或UI系统，状态机由有限数量的状态组成，同一时间只有一个状态处于激活。

系统特殊之处在于能根据条件切换状态，这样的条件可以是按钮按下，或更复杂的事件如移动特定距离，为了更直观理解，我们可以简单绘制状态图，再次以未支持下蹲或俯冲的玩家控制器为例，我们只剩下三个简单状态，地面，跳跃并下落，我们从地面状态开始，处于地面状态时，我们的角色可以在行走动画中左右移动，或静止并播放闲置动画，角色还可以选择按下跳跃按钮，当这种情况发生时，我们需要从地面状态过渡到跳跃状态，因此我们用箭头表示这一状态转换，接下来是跳跃状态，角色仍可左右移动，但应以逐渐减小的向上速度开始，跳跃状态应播放跳跃动画，有两个条件会触发过渡到空中状态，要么释放跳跃按钮，或到达跳跃顶点最后在空中状态，角色应再次能够左右移动，下落速度应随时间增加，落地时播放下落动画，我们回到地面状态完成三态切换，这个状态机设计看起来很稳固，但我们遗漏了一个容易被忽略的转换，你能指出当前缺少哪个吗，当我们从平台边缘行走时，可以漂浮在空中行走，这是因为未添加地面到空中状态的转换，离开地面时，状态机设计并无绝对对错，在设计状态机时，我常将状态分为地面，下落和跳跃状态，但也可以将地面状态拆分为，例如，两个不同状态，例如，闲置与行走状态，仅用卡片和箭头即可简单表示状态机，但代码中它们是什么样呢，需要一个变量跟踪状态值。

在游戏循环中，只需检查该值是否等于某个数字，若为真则运行对应数字的代码，这些数字代表不同状态，目前我们仅定义了状态0，状态1和状态2，但可定义所需任意数量状态，这已是状态机最基础形式，让我们通过实例实践，以这个例子，使用地面，跳跃和下落状态创建基础角色控制器，就像之前一样创建一个变量，我们将其命名为active_state以便跟踪数字，这将代表游戏循环中当前激活的状态在第一行，我在局部变量中捕获玩家输入，在下方我们将编写实际的状态机部分以确定应运行哪个状态，我们检查active_state的值是否为零，一或二，让我们将零关联到地面状态，一对应跳跃状态，二对应下落状态，然后为每个状态定义移动方式，通过调整玩家的速度，所有三个状态都应包含左右移动，这会导致部分代码重复，不过没关系，跳跃状态应产生向上运动，下落状态应受重力影响，然后定义状态之间的转换，我们只需将active_state变量赋新值即可创建转换，在地面状态时，当按下跳跃按钮时，应转换到跳跃状态，离开地面行走边缘时，应转换到下落状态，在跳跃状态时赋值为二，与之前在画板上设计的状态机不同，我们通过直接转换到下落状态进行无条件过渡，因此将active_state设为二，这样一来，跳跃状态会像瞬时动作一样仅运行一帧，我们将为这个转换添加条件，在下落状态内部，再次接触地面时。

通过重置active_state为零返回地面状态，对于下方的Godot用户，在底部，添加move_and_slide函数，这样当我们在角色上使用此脚本时玩家开始移动，效果与之前相同可以移动，跳跃和下落，但这次我们使用了简单状态机设计实现，我们可以通过添加枚举来提升代码可读性，枚举通过用单词替代数字使代码更易读，所以添加名为states的枚举，我们可以添加地面，跳跃和下落三个值，然后代替设置active_state为零，改为设置为state.floor，对于每个if状态条件和转换，用对应的枚举值替换数字，最后一步提升可读性，这就是用match语句替换if语句，在其他语言中这可能被称为switch或case语句在我们的游戏循环中，我们匹配当前活动状态，然后通过添加额外缩进来调整该代码块的范围，并仅用对应的状态值替换if语句，当你对比更新后的代码与旧代码，可以看出代码整洁了许多，现在我们为其添加了枚举类型，更容易识别当前状态，枚举值本身已提供了状态的简短描述，这种所谓的枚举状态机是编写整洁代码的绝佳第一步，但它仍存在一个小问题，如果我们想为其添加动画，添加行走和待机动画很简单因为它们会无限循环，当方向值为行走时播放行走动画，当方向值为空闲时不播放动画，但一次性动画怎么办，如果我们想在false状态下添加下落动画，游戏循环会每帧调用该动画。

这会导致下落动画重复播放，尽管本不该如此，我们需要一种仅调用一次下落动画的方法，也许你已经想到在切换状态前直接播放动画的解决方案，但这需要为每个进入false状态的过渡调用动画，如果你有多个状态过渡到false状态，会写出大量重复代码，幸运的是这个问题有更好的优雅解决方案，我们将使用状态切换来执行一次性操作，而不是直接赋值活动状态变量，我们将使用switch_state函数，它接受要切换到的目标状态作为参数，并像平常一样赋值给活动状态，然后创建与游戏循环相同的match结构，从而确定状态切换时应执行的操作，例如，当切换到false状态时，我们希望在游戏循环中播放下落动画，现在只需将所有直接赋值活动状态的操作替换为switch_state函数调用，目前，跳跃状态仍作为瞬时状态直接切换到false状态无需等待条件，跳跃状态的正确行为应是在释放跳跃键时开始下落，或当玩家达到跳跃顶点时，可通过比较y轴速度是否为零来判断，由于我们现在保持在跳跃状态，切换到false状态前也应重置y轴速度，不再在跳跃状态期间施加上升动作，现在可在切换到跳跃状态时施加上升动作，我们还可以调用跳跃动画，我们刚添加的代码片段，充分展示了状态机及其过渡的强大之处，跳跃到假状态的延迟过渡使我们对跳跃高度有更多控制。

新的开关日期函数非常适合在状态间添加一次性操作作为收尾，确保玩家可以通过翻转精灵在每个日期内转身，检查方向是否持有值，当该值小于零时翻转精灵，这构成了极其稳固的基础，此结构非常易于扩展，只需五步即可添加新状态，假设我们要添加蹲下动作，首先确定蹲下在状态机设计中的位置，当我们查看设计图板时，我认为蹲下只能从地面状态执行，按下蹲下按钮后，当然如果再次释放蹲下按钮，我们将返回地面状态，当我们执行蹲下动作时，会继续向行走方向滑动一段，我们可能会从平台上掉落，从蹲下状态开始，也可以向初始状态过渡作为第一步，将蹲下添加到状态枚举中，作为第二步，确定进入蹲下状态时应执行的操作，只需播放蹲下动画即可，第三步，在游戏循环中定义持续向前滑动的行为，只需将当前x速度乘以0.9，因此每帧，水平速度会轻微减少，在第一步中，添加从该状态到其他状态的过渡，如果我们不再接触地面，应切换到假状态，否则如果释放蹲下按钮，应切换到地面状态作为最后一步，添加其他状态到此状态的过渡，在我们的情况中，只有地面状态应过渡到蹲下状态，当按下蹲下按钮时，这将实现预期的蹲下行为，只是出于趣味，让我们30秒后添加潜水动作，在进入潜水状态时将潜水添加到枚举，播放潜水动画，将x速度设为输入方向。

乘以1.5倍行走速度，y速度设为跳跃速度的一半，在游戏循环中，我们应用渐变，并切换到地面状态，当接触地面时，最后添加从跳跃和下落状态按下潜水按钮的过渡，当方向具有值时，就这样我们有了一个功能性的潜水，这个枚举状态机是学习编程的绝佳方式，但它并不是唯一的状态机类型，还有一种更高级的基于继承的状态机，比这个复杂一些，它也被称为状态模式，所以请在评论区告诉我，如果你也希望我讲解这个主题，现在你已经掌握了基本状态机的内外运作，为什么不试试用新技能实现一个像我视频里那样，就像我在视频中创建的角色控制器，比如《空洞骑士：丝之歌》中使用的角色。



--- 由 vCaptions 生成 ---