---
url: https://www.youtube.com/watch?v=3g-N_M8PN0U
tags:
  - video
status: readed
date: 2026-08-17T18:06:43+08:00
---
![Destructible Smooth Terrain Using Voxel Tools | Godot 4](https://www.youtube.com/watch?v=3g-N_M8PN0U)

https://www.youtube.com/watch?v=3g-N_M8PN0U
无作者 

[章节] Intro

[音乐] 这里介绍如何使用Godot 体素工具插件创建基于Vel 的平滑地形，该地形可供玩家编辑并支持多种材质。这个插件可能是目前最好的Godot 体素插件。现在让我们开始吧。

[章节] Download the add-on

首先，你需要下载这个插件。前往Godbox 的GitHub 仓库，链接在描述中，以及我稍后会提到的所有资源链接。点击“ 发布” 并下载最新版本。这个插件的一个缺点是它是一个模块，这意味着它是Godot 的一个自定义版本，而不是一个可以添加到项目中的插件。

[章节] Adding terrain

不过，这是一个新项目。我从Godot 的体素工具版本中打开一个新项目，添加一个Vel LOD 列车来创建一个带有LOD 的列车。然后在生成器属性中，有多个生成器可供选择。生成器决定了列车将以何种形状生成。现在，将其设置为“ 体素生成器噪声2D”。确保测量设置为“ 体素测量平移”，以获得平滑地形。现在你应该可以看到一个高大的地图地形。顺便说一下，如果你更改……您可以在此处重新生成地形，使其在场景运行时可玩。

[章节] Player controller

当然，我们需要一个玩家控制器。我直接使用了资源库中的随机玩家控制器。为了让火车围绕玩家加载，请为其添加Vel Viewer。

[章节] Terrain editing

现在，我们让玩家可以编辑地形。为此，首先我们向摄像机添加一条射线投射，然后将目标设置为前方5 米。在玩家脚本中，我们获取对vcel 地形的引用，然后获取对trins vle 工具的引用（ 这是一个辅助类，可以轻松访问和修改Vel），以及对rast 的引用。然后在物理处理函数中获取rast 的端点。如果按下鼠标右键，则将Vel 工具模式设置为Vel 工具移除并调用Vel 工具。创建一个球体，球心位于rcast 的终点，半径设置为2，强度设置为point2。对添加地形也进行同样的操作，但将模式切换到添加。现在玩家可以修改地形了。

[章节] Saving terrain

修改地形时可能会遇到的一个问题是，当你卸载并重新加载地形时，修改的部分会消失。这是因为地形不安全。为了解决这个问题，我们向地形添加一个流。Vel 流允许将体素数据保存到文件或目录结构中，并进行加载。有多种选项可供选择，你可以自己查看它们的作用，但我将选择最简单的Vel 流内存，它将Vel 数据保存到内存而不是磁盘。

[章节] Terrain material

现在，对于火车的材质，你需要使用着色材质。为了添加用于多个纹理的着色器，以及另一个必要的着色器来为地形分配纹理。你可以基于网格数据（ 例如法线矢量） 来设置纹理，但在我们的例子中，我们希望根据每个体素的纹理索引来设置纹理，以便我们可以选择要使用的纹理。为此，我们使用文档中的这个着色器。不要删除用于消除这些瑕疵的默认代码。纹理代码需要一个纹理数组作为输入。我将使用像素艺术作为纹理，因此我添加了这个额外的过滤器。纹理数组可以通过脚本创建，所以将一个新脚本附加到体素地形。然后在ready 函数中加载地形纹理，将它们添加到纹理数组，并将纹理数组添加到着色器。将其设置为工具脚本，以便它可以在编辑器中运行。然后在drain measure 属性中，将纹理模数设置为4，BL blend over 16 texture。现在，如果我们重新生成地形或运行场景，您将看到数组中的第一个纹理。为了让火车生成不同的纹理，我们需要使用自定义生成器，

[章节] Custom generator

因为Vel 生成器噪声太有限。有两种自定义生成器可供选择：生成器脚本或生成器图。生成器脚本可以用GD 脚本、C 或C++ 编写。我们不会使用C 和C++ 生成器，因为它们设置起来太复杂。我们也不会使用G 描述符生成器。我们将使用SCPH 生成器，因为它速度与C++ 生成器类似，而且设置起来更简单。虽然它的功能有限，只有一些基本指令，但足以生成简单的地形。现在添加Bole 生成器图。默认情况下，生成器图未设置为生成平坦的地形。我们将像噪声生成器一样重新创建高空地形。因此，添加噪声Tod。您可以点击它来更改噪声设置。如果需要，可以将噪声输出乘以20，使地形更加颠簸。然后将其连接到SDF 平面的高度输入，以获取当前速度的S 距离值。之后，将SDF 值限制在-1 到1 之间，这样无论距离表面多远，地形编辑强度都将保持一致。最后，将其连接到输出SDF。现在您就得到了一个类似于噪声生成器的地形。这次我使用的是图生成器。接下来，要向地形添加多个纹理，

[章节] Multiple terrain texture

请添加一个输出单纹理节点。这将根据纹理索引为速度设置纹理。我们将根据3D 噪声设置纹理，因此添加3D 噪声。将Noe 连接到选择节点，选择Noe 的工作原理是：如果输入T 低于阈值，则返回A；否则返回B。因此，将A 和B 设置为要使用的纹理索引，然后将其连接到输出单个纹理。现在，地形上应该有石头和草地。

[章节] Painting texture

接下来要添加的功能是在玩家脚本中选择要使用的材料。添加地形后，将Vel 工具模式切换到纹理绘制，然后调用Vel 工具。在RASS 端点处绘制半径为2.5 的球体。之所以将添加颜料的半径设置为2.5 而不是像添加雨滴那样设置为2，是为了确保新添加的Trin 完全被绘制。无论如何，默认情况下，绘制代码会使用第一个纹理绘制Trin，要更改纹理，我们更改Vel 工具。我刚刚设置了Simplicity 的纹理索引，让它在按下E 键时位于三个纹理之间，

[章节] Outro

就这样，我们完成了。要了解更多关于此插件的信息，请查看文档或加入Discord 服务器寻求帮助。



--- 由 vCaptions 生成 ---