
```dataview
table status
from "Read It Later" or "Watch It Later"
where status = "Unsorted"
sort file.name asc

```





```dataview
TABLE file.name AS "文件名", length(file.name) AS "字符数"
FROM ""
WHERE length(file.name) > 20
SORT length(file.name) DESC
```

