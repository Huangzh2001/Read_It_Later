---
url: "https://www.youtube.com/watch?v=ru_oz09Zo-s&list=WL&index=3"
tags:
  - "video"
status: "readed"
date: "2026-08-18T21:45:57+08:00"
---
![500 million voxels/sec - Voxel engine optimizations](https://www.youtube.com/watch?v=ru_oz09Zo-s&list=WL&index=3)
500 million voxels/sec - Voxel engine optimizations
https://www.youtube.com/watch?v=ru_oz09Zo-s&list=WL&index=3
无作者 

我花了几个月的时间优化我的体素引擎，现在它真的可以称得上是速度飞快了。在加载同一个世界（ 音乐） 时，我的性能提高了20 倍，为了做到这一点，我几乎尝试了所有编程技巧。极限边界检查、位操作、缓存局部性、噪声上采样、噪声缓存、游程解码、查找表、EOL。我通过对体素引擎进行一些非常不寻常的操作，发现了一些非常疯狂的优化方法，而我从未听人提起过这些方法。我尝试过一些方法，结果却适得其反，导致性能更差。为了实现一个远大的目标，我做了很多实验。我能让我的体素引擎比《 方块世界》 更快吗？Cube World 是一款优化得非常好的游戏，将我的引擎与Cube World 进行比较，我加载块的速度慢得令人尴尬，而我以前还觉得这没问题。你大概可以用我的引擎开发游戏。它不是最快的。它占用的内存有点多，但是你看，我不想过早地优化我的代码。我以前也是这么想的，直到我在体素引擎中实现了一个名为基于遮挡的颜色渐变的新渲染功能。这是一种渲染技术，它通过赋予体素类型从单一颜色到从环境光遮蔽中选取的颜色渐变，使体素世界从单调变得惊艳美丽。我会在下一期视频中深入讲解这项技术，敬请期待。随着这项新功能的实现，我原本已经勉强达到可以发布游戏的标准，现在却彻底超出了这条界限，出现了问题。

然而，色彩（ 音乐） 渐变并不是问题的关键所在。性能下降幅度不超过10%（ 音乐速度），但仅仅是性能上的小幅下降，就让音乐从尚可接受变成了令人质疑我是否可以发布作品。它表明我的代码库基础已经不够牢固了。是时候优化每一行代码了。[音乐] 我的性能问题的根源在于数据生成，也就是生成世界形状的代码。我一直以来都在避免进行数据优化，因为我今天要向你们展示的一些优化方法虽然很容易理解，但实施起来却可能异常困难。在过去的几个月里，我完全重写了我的体素引擎，顺便说一下，它是用Rust 编程语言和Bevy 游戏引擎编写的。让我们来看优化方法一，也是最强大的优化方法——极限边界检查。在向您展示其工作原理之前，左边和右边在逻辑上是完全相同的形状。唯一的区别在于左边的表达式使用了极限边界检查，而右边的表达式没有使用。左边那个已经完成了。只用了2 秒钟。正确的那个需要29 秒。除了左侧的形状逻辑可以避免进行大量繁重的计算之外，其他形状逻辑完全相同，在这种情况下速度提高了15 倍。那么，它是如何运作的呢？我们先来看一个例子。该函数将返回一块体素。这是一个由32×32 ×32 体素组成的平面矢量图。在这个for 循环中，我们遍历体素的每个可能的XYZ 位置，

但是与其创建一个大的XYZ for 循环嵌套，不如使用一个for 循环，并简单地根据索引计算XYZ 。为了确定体素是草地还是空气，我们将使用高度图。我们将这个函数称为噪声函数。在这个例子中，噪声函数返回一个介于0 到1 之间的值。然后我们可以将这个噪声值乘以10。如果体素的Y 坐标低于生成的这个高度，我们将返回草地。如果它位于上方，我们将创建一个空的空气体素。好的。让我们来实现极限边界检查。如果我们正在检查的体素的Y 值为18，考虑到我们的高度图只能达到最大值10，我们不需要调用这个噪声函数，因为我们知道它将是一个空气块。同样地，如果Y 坐标低于零，我们就知道我们肯定在地下。因此，我们可以实现一个检查，如果超出我们的范围，我们可以完全避免调用噪声函数。基线耗时300 微秒，而使用极限边界检查[ 音乐] 耗时约100 微秒，速度提高了三倍。显然，如果我们创建的数据块完全超出边界，速度会更快。我们说的是245 纳秒，非常快。但是，生活并非总是阳光灿烂、充满美好。假设我们再添加一个噪声函数。在调整这些变量时，我们需要手动更新边界。我们采用的形状逻辑越复杂，情况就越糟糕。你很可能会用到域扭曲技术。您可以设置一些阈值。您可以根据我的位置应用渐变来减少尺度噪声。

根据你应用塑形逻辑的数学方法，边界的行为也会有所不同。手动计算边界很麻烦。现在，如果我们仔细观察这个例子，就会发现它实际上并没有计算出完美的界限。我们进行一次if 检查，以防止在这种情况下出现两次噪音呼叫。根据第一个噪声的值，我们或许可以避免调用第二个噪声。实际上，在这种情况下，我会这样写出一个更准确的界限。这样做虽然执行速度更快，但代码可读性却变得极其糟糕。作为世界的设计师，我不想在意那些极端的界限。我只想调用两个噪声函数并将它们相加。但是，这种优化需要人工干预。当然，除非你愿意花费数月时间构建一个解释器来完全自动化这种优化。有可能。这真的非常难，但我还是做到了。我不再使用Rust 代码来处理体素塑形逻辑。现在它已被序列化到一个文件中。首先，我定义我需要的噪声参数。然后，我列出了这些层级，每一层级都可以定义塑造世界的不同方式。让我们重新创建这个原始的塑形示例。我们的噪声源将是二维噪声。我们的映射范围将从0 到10。输出目标将是高度。这就是重现相同塑形逻辑所需的全部内容。正如你所看到的，极端界限的概念在这里完全找不到踪影。这些边界值隐藏在我的代码中，由程序自动计算。这一切都取决于你如何设置这些图层。

如果我把山峰建得更高，极限边界也会相应增加。我们无需考虑就能获得极限边界检查的性能。真是不可思议。我怀疑Hytale 也在做类似的事情。他们有基于编辑器的编辑器。我很好奇他们是否也在进行某种极限边界检查，因为他们的引擎速度非常快。唯一的缺点是它不如手动编写代码那样富有表现力。你现在就可以做一些很酷的事情。我可以制作高度图。我可以进行域扭曲。我可以控制密度并制造洞穴。你可以通过阈值分割和域扭曲来创建一些非常酷的柱状图。当然，你可以根据需要使用任意数量的图层。但是，更复杂的形状逻辑很可能很难（ 如果不是不可能的话） 用我目前设计的系统方式来表达。正因如此，我才有些犹豫是否要深入研究我的实施方案，因为它很容易发生变化。在制作这段视频的同时，我仍在探索这项技术。实际上，在体素引擎早期阶段，我不会考虑进行极限边界检查的优化，因为要么手动进行，这会增加维护难度和代码复杂性，要​​ 么尝试通过序列化塑形逻辑来实现自动化，但这非常困难，就像构建一个解释器一样。这是一个巨大的架构挑战，但如果你想从快到极快，那么自动化极限边界技术绝对值得探索。每个体素引擎都应该使用两种优化技术：噪声上采样和噪声缓存。它们易于实施，而且如果你想快速发展，

它们基本上是必不可少的。由于这些都是众所周知的技巧，我将简要介绍一下。目标是停止对每个体素坐标都调用这些昂贵的噪声函数。极端边界可以减少大范围内的呼叫，但当我们在极端边界内时，我们可以通过上采样进一步减少噪声呼叫。与其对每个体素进行采样，我们可以每隔一秒、每隔四秒、每隔八秒、每隔十六分之一体素进行采样，然后填充没有噪声数据的空白区域，我们可以对生成的点进行插值。当然，我们进行上采样越多，噪声中的细节就越少，但是上采样四倍后，质量却出奇地好，至于性能嘛，我们来看看数学计算。如果我们使用32 个立方体块，并以四倍上采样，我们只需要对噪声进行729 次采样。与对每个体素进行采样相比，噪声调用减少了45 倍。但别太兴奋，这并不意味着性能会提高45 倍，因为上采样需要在这些点之间进行插值。用代码实现速度很快，但成本并不低。让我们来看一些使用3D 噪声的实际基准测试。不使用上采样的基准测试大约需要7 毫秒才能创建一个64 立方体块。改为两倍上采样，现在需要2.4 毫秒。我们在几乎没有损失质量的情况下，将性能提高了三倍。规模扩大到四倍，现在只需1.4 毫秒。放大8 倍，耗时1 毫秒；再放大16 倍，耗时1 毫秒。正如你所看到的，性能提升并非线性增长。

从8 倍放大到16 倍，我们几乎没有看到任何区别，这是因为我们的代码仍然需要对每个体素的噪声值进行插值，而我们无法做到这一点。在大多数情况下，使用四倍上采样不会损失太多质量，还能获得500% 的性能提升。噪声缓存的关键在于：永远不要问同样的问题两次。在这段代码中，我们遍历每个XYZ 坐标，如果你仔细观察，就会发现效率极低。由于X 和Z 位置在Y 轴上移动时不会改变，因此我们连续计算了完全相同的值32 次。因为输入相同，所以输出也相同。因此，只需将噪声调用移到while 循环之外，我们就可以计算一次，然后重用结果。这样一来，噪声评估工作量立即减少了32 倍。非常好，但噪声缓存不仅仅在2D 高度图中有用。当不同生物群落采用不同的地形划分逻辑时，边界处的造价就会很高。为了使两个生物群落平滑融合，引擎通常需要同时运行完整的塑形逻辑，也就是同时运行两个生物群落的每一个噪声函数。然而，大多数生物群落通常具有非常相似的噪声参数。我们通常使用低频噪声来生成大型地形特征。我们使用中频噪声来增加变化。所以，我们可以在这里做些聪明的事。与其让每个生物群落独立计算这些图层，我们可以更聪明一些。如果底层噪声参数相同，我们可以在启动时计算一次这些噪声值，

然后将它们传递给每个相关生物群落的形状逻辑。以下是我在实践中如何实现这一点的。我有一个共享文件，您可以在其中定义全局可访问的噪声参数。如果我的森林生物群系塑形器和雪地生物群系塑形器都使用这个全局定义的噪声，我的引擎会烘焙这个噪声并将其分别传递给两个塑形器，从而有效地避免两次重新计算相同的噪声值。现在，生物群系混合的情况并不常见，因此这种优化仅适用于相邻生物群系的情况。接下来的优化可能会引起争议。嗯，至少这很不寻常。我还没见过有人这样做。我们通常将体素数据存储在一个大小为32 立方的平面向量中。但我现在不再那样做了。我将体素数据以游程编码形式存储。我的块大小是32 ×256×32。什么？那是什么？太疯狂了。停止。游程编码其实很简单。在这个二维示例中，使用游程编码，我们首先指定体素类型，然后指定高度。第一列表示为草四，气二，下一列表示为土二，草一，气三。与传统方法（ 即存储每个体素） 相比，这种方法会生成“ 草、草、草、草、空气、空气” 等等这样的结果。这听起来很浪费，不是吗？就内存而言，是的，非常如此。游程编码几乎不占用任何内存。因此，几乎所有体素游戏在保存到文件或通过网络发送世界数据时都会压缩体素数据。但几乎没有人使用游程编码作为运行时数据。除了我。

为什么没有人使用游程编码来处理运行时数据？随机内存访问和修改的计算速度比处理完整数据要慢得多。例如，如果我们需要询问X1 Y4 上的体素类型是什么。在游程编码中，我们需要遍历数据才能找到该位置。效率不高。但这还不是最糟糕的。修改后的版本更糟糕。如果你想在一个巨大的空中跑道中间放置一个泥土方块，你必须将该跑道分割开来，调整数组大小，然后插入两个新物品。与传统方法（ 存储每个体素） 相比，要获取或修改体素，我们只需对数组进行索引即可。很简单。速度很快。如果游程编码客观上更复杂、随机内存访问速度更慢，那我为什么还要使用它呢？嗯，我发现使用游程编码确实能带来一些意想不到的性能提升。在某些情况下，这种方法比全体积体素更快。以下是其优势。要将我们的体素世界保存到文件中，或者如果您想通过网络发送体素数据，大多数引擎都必须暂停并将数据烘焙成这种压缩格式。但由于我的运行时数据是这种压缩格式，将其保存到磁盘基本上就相当于内存转储。这让我的生活轻松多了。我们还能在低内存占用的情况下获得巨大的渲染距离。在如今内存严重不足的情况下，您可以放心使用超大的渲染距离，而不用担心内存耗尽。这里渲染距离为36 个区块，仅占用400 兆字节。如果不压缩，这个文件将有15 GB。

从技术上讲，这取决于体素数据类型。如果使用每个体素8 位（ 我们的基本情况），这个世界将占用7 GB 的空间。但是，你可能会想，真的需要将每个数据块都存储在运行时内存中吗？当然，我们需要它在视觉上保持可见，这样你就可以加载一次，但你可以卸载距离很远的区块，并保持它们仍然可见。是的，理论上可以这样做，但是块管理系统的编程会非常困难。确定何时加载数据、何时加载网格就已经够难了。我不知道，也许有可能，但对我来说没关系，因为我使用游程编码，数据量非常小。我的意思是，拜托，400 MB？世界很大。另一个不寻常的好处是，游程编码使我能够使用32 x 256 x 32 的块。它的垂直尺寸比大多数引擎都要大得多，而且这一点比你想象的更重要，因为更大的区块大小可以大幅降低区块管理开销。负责加载和卸载数据块的系统需要扫描的数据块位置要少得多。当我以极快的速度飞行时，扫描仪也能很好地跟上。另一个好处是噪声缓存。2D 高度图会被缓存并在Y 轴上重复使用，但这种噪声缓存仅发生在此块内。如果我垂直堆叠多个数据块，它们实际上并不会共享这个噪声缓存。通过将块的高度增加八倍，可以再重复使用高度图值八次，从而有效地减少采样次数八次。我在计算应该在哪里放置哪个生物群系时，也采用了同样的方法。

生物群落由X 和Z 坐标决定。这个世界的生物群落数据可以获取一次，并重复使用256 次。现在，游程编码和极端边界结合起来是有好处的。假设我们的极限边界发现，当Y 小于200 时，我们应该让世界充满草地。我们可以用一条记录“grass 200” 立即写入数据。在传统的数组中，这需要使用for 循环遍历每个条目。大文件写入速度非常快。游程编码最酷的功能之一就是极快的表面检测。如果我们需要找到想要生成树木、岩石或NPC 的位置，这被称为地表特征，我们可以非常快速地找到这些位置。遍历我们的游程编码体素，我们可以得到每个块发生变化的位置。我们不必遍历整个体素体积。我们甚至可以立即计算出洞穴的高度，因为它就在数据中。现在，在你着手重写整个体素引擎以使用游程编码之前，我必须说这仍然是一个巨大的实验。我还没有时间重新实现表面特征，例如树木之类的东西，这需要随机内存访问，而且我还没有时间对大型世界修改进行压力测试。因此，使用游程编码的这些独特优势是否值得，将会非常有趣。目前我的代码缓存效率非常高，但这种情况可能很快就会改变。够了，别再唠叨了。给我提供那些劲爆的数据。它到底有多快？这是30,000 个区块。那大约有80 亿个体素，我的引擎需要14.4 秒才能加载这些数据并进行网格划分。

这意味着我的引擎每秒可以处理五亿个体素。非常令人印象深刻，但之前的速度有多快？嗯，在我之前的引擎中，加载38,000 个区块需要61 秒。我之前的引擎使用32 立方的区块大小，这意味着加载10 亿体素的体积需要61 秒。我之前的引擎每秒可以处理2000 万个体素，而我的新引擎每秒可以处理5 亿个体素。我的新引擎速度提高了25 倍。显然，我还没有实现树形结构，但这是一个非常好的基准。极端边界、噪声上采样、噪声缓存和游程编码是我达到这一目标的四种主要技术[ 音乐] 。最不可思议的是，我知道还有办法加快这个过程。但在我进一步优化之前，更应该先构建使用该系统的真实世界，因为只有这样我们才能真正知道它在现实场景中是否有效。[音乐].



--- 由 vCaptions 生成 ---