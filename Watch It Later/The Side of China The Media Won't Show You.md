---
url: https://www.youtube.com/watch?v=fci9MQ3VZ_I
tags:
  - video
status: readed
date: 2026-06-04T21:19:38+08:00
---
![The Side of China The Media Won't Show You](https://www.youtube.com/watch?v=fci9MQ3VZ_I)
Giving Personality to Procedural Animations using Math
https://www.youtube.com/watch?v=KPoeNZZ6H4s
无作者 

[章节] intro

在传统的矢量动画中， 有一个叫做插值曲线的概念，它描述了动画关键帧之间的过渡方式。根据这条曲线的形状， 我们可以控制运动，使其缓慢缓入、缓出，甚至 提前并超越关键帧。动画曲线的选择会极大地影响 运动中的惯性和能量感。这使得动画师 在描述运动时拥有很大的艺术表现力，同时又能有效地控制关键帧的数量。然而，在程序动画中，完全 摒弃关键帧的概念非常强大, ，它可以让系统根据 实际情况自由移动。这种适应性是将 程序动画应用于游戏的主要原因之一。例如，在我正在开发的游戏中，我有 一个小机器人角色，它, 使用逆运动学来响应 实时移动和改变方向的请求，从而移动身体和腿部。我在这里用鼠标控制它， 你可以在网上找到相关的教程，以及如何 实现类似的功能。在游戏中，这种灵活性同样适用。 这使得 角色能够立即响应不断变化的游戏情境。这很棒，但由于我们 没有这些输入的关键帧，因此无法真正使用插值曲线 来添加惯性感。我们真正想要的是能够使用 类似于插值曲线的直观抽象来实现类似的效果，同时又不损失无 关键帧系统的动态响应能力。

[章节] second order systems

首先，让我们 更精确地描述一下我们想要解决的问题。给定 来自游戏世界的动态变化的输入 x，我们希望生成一个动态输出 y， 该输出跟踪输入 x，但又具有, 某种特征来传达 我们希望的运动感觉。在最简单的情况下，y=x，我们的系统会 严格遵循我们给它的任何输入。这本身并没有太大用处， 但它是一个很好的起点。无论我们对这个等式进行什么更改，我们都 希望确保从长远来看，当 x 不再变化时， y 最终应该稳定在 x。为了引入有趣的动态效果，让我们添加一些速度和 加速度项，这里, 用时间的一阶和二阶导数表示 ，并分别乘以 k1、k2 和 k3 的值。这里的点号表示法只是 时间导数的简写方式，你可能也见过这样的写法，或者这样的写法。这种涉及 二阶时间导数的关系被称为二阶系统。现实世界中的机械运动往往 是二阶的，你可以将其理解为, 力作 用于加速度（位置的二阶导数）, ，正如牛顿第二定律所描述的那样。 这些力通常源于刚度（, 它是位移的函数）或 摩擦力（它是速度的函数） ，因此我们选择了这些项。例如，这是刚体沿一个轴被线性阻尼弹簧拖动时的运动方程。这是 Unity 中 SmoothDamp 函数的运动方程。通过调整 k1、k2 和 k3 的值，我们可以生成各种各样, 有趣的行为，但作为一名艺术家， 这可能不太理想。

首先，让我们简化可视化，观察系统对 输入突变的响应。由此, 产生的输出称为 阶跃响应，这是一种, 可视化系统动力学的标准方法，能够 捕捉系统行为的本质。接下来，我们将使用 k1、k2 和 k3 定义三个新变量：f、zeta 和 r。我们将使用 f、zeta 和 r （稍后我会解释） 作为系统运动的设计参数。在, 底层，我们可以利用它们 来计算 k1、k2 和 k3。在 f、zeta 和 r 的这种新的参数化中， 每个项都控制着一些, 我们可以合理理解的东西。f 是系统的固有频率， 单位为赫兹 (Hz)，即每秒周期数。它描述了系统对 输入变化的响应速度，以及, 系统倾向于振动的频率，但 不影响最终运动的形状。zeta, 被称为…… 阻尼系数描述了系统如何 稳定在目标位置。当 zeta 为 0 时，振动永不 停止，系统无阻尼。当 zeta, 值介于 0 和 1 之间时，系统 欠阻尼，振动幅度取决于 zeta 的大小。当 zeta 大于 1 时，系统不会振动，而是缓慢地向目标 x 方向稳定，具体速度取决于 zeta 的大小。Unity, 中的 SmoothDamp 函数使用 zeta 等于 1 的值，这被称为临界阻尼。r, 是一个控制 系统初始响应的值。当 r 为 0 时，系统需要一些时间才能 从静止开始加速。当 r 为正值时，系统 对 x 方向的运动立即做出反应。当 r 大于 1 时， 系统会过冲目标位置。

当 r 为负值时，系统 会提前响应运动。在模拟机械连接时， r 的典型值为 2。现在，作为艺术家，我们已经拥有了 开始探索运动特性所需的一切。 程序动画对象。如果我们想控制 系统的稳定行为，可以调整 zeta 值；如果我们想控制 系统的初始响应，可以调整 r 值；如果我们想控制 系统行为的快慢，可以调整 f 值。回到这个机器人角色，可以 看到我使用了欠阻尼运动来控制, 身体，zeta 值为 0.5，r 值为 -2，以便 在快速运动前赋予它一些预判。为了增加一些趣味性，我将 身体向目标位置倾斜，进一步暗示运动背后的意图。 我希望​​转向是平滑的，因此对于身体, 方向，我使用了 zeta 值为 1 和 r 值为 0。 我对头部方向也做了同样的处理，但使用了更小的 f 值，使其滞后于 身体，作为一种辅助运动。电缆的动画使用了一个小技巧， 我之前在推特上解释过。

[章节] implementation

现在让我们来看一种 实时求解这个方程的方法，以便我们可以开始在实践中使用它。有很多方法可以进行数值求解。 求解微分方程（, 例如我们提出的二阶方程）的方法有很多，其中一种特别 直接的方法是欧拉方法。今天，我将介绍一种 称为半隐式欧拉方法的变体，对于我们的系统，它恰好与 更复杂的Verlet积分具有相同的精度。首先，我们需要分配一些状态 变量：位置和速度。这些是我们对 y及其一阶导数的估计值。在第0步，它们需要 初始化为一些初始值。现在，当游戏运行时，我们希望在 每一帧迭代地更新这些变量。我们将 帧之间的时间间隔称为T。首先，我们通过 将上一次迭代的位置加上T乘以速度来更新位置估计值。接下来，我们通过将上一次 迭代的速度加上T乘以加速度来更新速度。为了计算这个加速度， 回顾一下我们的运动方程。我们可以在这里求解加速度，并代入我们最新的 位置和速度估计值。请注意，在半隐式欧拉方法中，我们使用 更新后的位置来计算速度，这与常规欧拉方法略有不同，常规欧拉 方法使用上一帧的位置。 位置：如果输入速度 x 点 未知，我们也可以使用历史测量值来估计它, ，例如，将其近似 为自上一个样本以来的平均速度。该算法 几乎可以逐行转换为代码。

[章节] testing

如果我们将其集成到游戏中，我们 会发现它确实按预期工作。我已经连接了代码，允许 实时更改 f、zeta 和 r，系统也做出了相应的响应。不过，经过一些实验后，我们会遇到一个大问题：如果将, 共振频率 f 相 对于帧速率设置得太高，系统将变得完全不稳定，并无限循环。虽然可以简单地将 f 保持在 足够低的水平，从而降低此类故障发生的可能性，但, 在游戏引擎中，我们更希望能够更 有力地保证在极端, 情况下（例如延迟峰值导致时间步长远超预期）不会发生此类问题。这是一个可以解决的问题，但 要正确理解它，我们需要进行更深入的技术分析。

[章节] stability

原因如下： 像欧拉方法这样的物理求解器之所以 会变得不稳定，本质上是因为, 它们是反馈系统， 一次迭代的输出（这里是 y 及其导数） 会被反馈到后续的迭代中。当帧之间的时间步 长相对于参数过大时，误差会随着时间的推移而累积。超过临界阈值后，这些误差 会开始自我叠加，迅速导致灾难性故障。为了计算这个阈值，我们需要将 问题组织成更标准的形式。我们想要做的是用第 n 帧的 状态变量来表示第 n+1 帧的状态变量值。我们可以通过将第一个方程代入第二个方程来实现这一点 ，这样两个方程的, 右侧都包含了第 n 帧的 y 及其导数。现在让我们展开并合并同类项。这是一个线性方程组， 我们可以用矩阵形式来表示。这样表示的方程组被称为 状态空间表示。这个矩阵 A，被称为 状态转移矩阵，描述了状态变量的每次迭代是如何进行的。 影响下一次迭代。直观地说，你可以想象，如果 A 不会导致状态变量增长，反馈就会保持稳定。如果 A 是一个数字而不是矩阵，那么更容易理解 它对系统的影响。例如，在这个只有 一个状态变量且没有外力的简单系统中，每次迭代都等于 前一次迭代乘以 A。当 A 的模小于 1 时，每次迭代中 y 的模, 都会小于前一次迭代， 从而使系统随着时间的推移趋于稳定。

当 A 的模大于 1 时， 每次迭代中 y 的模都会大于前一次迭代，因此 系统会迅速失控增长。这正是我们想要避免的。在我们的系统中，A 是一个 2x2 矩阵，它没有可以直接与 1 比较的模。相反，它有两个我们可以视为 独立模的量，称为特征值。我们在这里不深入探讨，但只需 说明这两个特征值……  z1 和 z2 是, 通过求解该方程的两个解计算得到的 ，它们的绝对值必须小于 1，系统才能随时间稳定下来。计算方法如下：展开后得到 z 的二次多项式，我们可以使用求根公式求解。当 该表达式的绝对值小于 1 时，系统将稳定。解这个不等式得到 以下约束：时间步长 T 必须小于 4 乘以 k2 加 k1 的平方再减去 k1 的平方根。在我们的代码中，我们可以计算这个 最大稳定时间步长 T_critical，并在游戏运行时将其与时间步长进行比较。如果时间步长过大，我们可以将其分割成小于 稳定性阈值的较小步长。缩小步长需要 每帧执行更多计算。为了避免这种情况，我们或许可以 通过降低动力学速度来解决问题。在这种情况下，与其约束 T，不如求解 k2 并约束它。将 k2 限制在这个值以上在物理上是不 正确的，但请记住，我们在, 这里的目标只是防止系统崩溃。 灾难性的失败， 无法产生精确的物理模拟。

[章节] conclusion

当然， 你可以将这种分析延伸到更深奥的领域，在本视频的早期版本中 ，我确实更进一步。例如， 当频率, 足够高时，帧间抖动行为就会悄然出现，这是由负 特征值引起的。因此，如果我们, 在分析过程中也考虑到这一点，就可以施加额外的 限制来确保这种情况不会发生。在我自己的实现中，如果 需要精度，我选择使用一种称为, 极零匹配的方法， 根据时间步长计算每帧的 k1 和 k2 值。这通常会 为非常快速的运动产生更精确的结果，但会带来额外的计算 成本，这可能并不适合所有应用。使用像我们的 fzeta r 这样的简单参数化运动模型背后的理念扩展了 一些有趣的可能。举个例子，我们可以调节 这些参数的值来传达游戏信息，这样 角色就可以, 根据他们的意识状态、 生命值、状态效果等使用不同的参数进行移动。这将有助于 通过以下方式向玩家传达情况：以一种成本低廉且易于迭代的方式实现运动。总之，我已经花了太多时间 制作这个视频而不是开发我的游戏了，所以希望这能成为一个通俗易懂 但又内容丰富的入门介绍，介绍, 一些有趣的概念，或者 展示一些, 数学在不寻常但有趣的背景下的应用。一如既往，感谢观看。



--- 由 vCaptions 生成 ---